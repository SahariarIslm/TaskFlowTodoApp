# TaskFlow Laravel App

This directory contains the Laravel backend and Vue frontend for TaskFlow.

For full project setup, installation, database, mail, Google OAuth, local run, build, and troubleshooting instructions, see the root README:

```text
../README.md
```

Quick local start after completing setup:

```bash
php artisan serve
npm run dev
```

Open:

```text
http://localhost:8000
```
